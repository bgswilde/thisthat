module.exports = {
    question_loop: id => {
      for (i = 0; i < id.length; i++) {
        
        const button = document.querySelector('.next-button')

        button.addEventListener('click')
        
      }
    }
}